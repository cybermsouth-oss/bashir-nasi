// ============================================
// BASHIRI NASI - MATCH CALENDAR (DISABLED)
// Calendar removed from homepage
// ============================================

document.addEventListener('DOMContentLoaded', function() {
    // Match Calendar disabled - not loading on homepage
    console.log('📅 Match Calendar: Disabled (not showing on homepage)');
});

var MatchCalendar = {
    init: function() {
        // Disabled
    }
};

window.MatchCalendar = MatchCalendar;